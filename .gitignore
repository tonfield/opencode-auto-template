node_modules/
.bun.lock
*.jsonc
*.json.bak
*.log
.DS_Store
opencode-notifier-state.json

# BEGIN opencode job surfaces
# Root job files (active + archive) are local episodic state — not tracked here.
# Root-anchored (/jobs/) so project/jobs/ (the template model) stays trackable.
# Downstream project repos initialized from project/ may track job markdown;
# the init command recommends adding exceptions there:
#   !jobs/
#   !jobs/*.md
/jobs/
# END opencode job surfaces
